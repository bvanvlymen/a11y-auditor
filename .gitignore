# dependencies
node_modules/

# local env — secrets live here, never committed
.env
.env*.local

# Terraform (from M1 onward) — state and vars can contain secrets
*.tfstate
*.tfstate.*
.terraform/
*.tfvars

# Lambda zip packaging artifact (M2) — rebuilt by terraform apply
infra/api.zip
